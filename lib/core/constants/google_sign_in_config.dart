/// The Firebase-generated OAuth "Web client" id (the `oauth_client` entry
/// with `client_type: 3` in `google-services.json`), passed as
/// `serverClientId` so Google Sign-In issues an ID token audienced for
/// Firebase Auth's backend to verify.
const String googleServerClientId =
    '660568940887-gs3ioh0nbtie90m3k8sjpfcfq1nafo6t.apps.googleusercontent.com';
